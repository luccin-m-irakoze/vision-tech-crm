package com.visiontechcrm.model;

/** Channels through which a sales rep / support engineer can talk to a customer. */
public enum InteractionType {
    CALL,
    EMAIL,
    MEETING,
    SITE_VISIT
}
